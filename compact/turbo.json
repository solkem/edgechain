{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["compact/**"],
      "env": ["COMPACT_HOME"],
      "inputs": ["src/**"],
      "cache": true
    }
  }
}
