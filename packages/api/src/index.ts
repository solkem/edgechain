/**
 * Shared constants used by workspace packages.
 *
 * NOTE: Runtime FL HTTP endpoints were consolidated into `/server`.
 * This package intentionally does not expose an HTTP server anymore.
 */
export const QuickStarterPrivateStateId = 'quick-starter-private-state';
